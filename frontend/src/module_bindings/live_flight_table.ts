// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.2.0 (commit fb41e50eb73573b70eea532aeb6158eaac06fae0).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { LiveFlight } from "./live_flight_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `live_flight`.
 *
 * Obtain a handle from the [`liveFlight`] property on [`RemoteTables`],
 * like `ctx.db.liveFlight`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.liveFlight.on_insert(...)`.
 */
export class LiveFlightTableHandle {
  tableCache: TableCache<LiveFlight>;

  constructor(tableCache: TableCache<LiveFlight>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<LiveFlight> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `flightId` unique index on the table `live_flight`,
   * which allows point queries on the field of the same name
   * via the [`LiveFlightFlightIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.liveFlight.flightId().find(...)`.
   *
   * Get a handle on the `flightId` unique index on the table `live_flight`.
   */
  flightId = {
    // Find the subscribed row whose `flightId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): LiveFlight | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.flightId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: LiveFlight) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: LiveFlight) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: LiveFlight) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: LiveFlight) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: LiveFlight, newRow: LiveFlight) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: LiveFlight, newRow: LiveFlight) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
